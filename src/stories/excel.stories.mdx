import { Meta, Story, Canvas } from '@storybook/addon-docs'
import ExportWord from './components/word.vue'

<Meta title="工具库/wps" component={ExportWord} />

export const Template = (args) => ({
  components: { ExportWord },
  setup() {
    return { args }
  },
  template: '<export-word v-bind="args" />'
})

<Story
  name="文档"
>
  {Template.bind({})}
</Story>

### 使用文档

- 使用方法

```vue
<script lang="ts" setup>
const exportFile = renderDoc({
  list: [
    { img: '/demo.png', name: '图片1' },
    { img: '/demo.png', name: '图片2' }
  ],
  url: '/doc-template.docx'
})
</script>
```
