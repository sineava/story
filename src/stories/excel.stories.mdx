import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import ExportWord from './components/export-word.vue'
import dedent from 'ts-dedent'

<Meta title="组件库/wps" component={ExportWord} />

export const Template = (args) => ({
  components: { ExportWord },
  setup() {
    return { args }
  },
  template: '<export-word v-bind="args" />'
})

# 文档组件

<Canvas>
  <Story 
    name="文档"
  >
    {Template.bind({})}
  </Story>
</Canvas>

- 导出word带图片

<Source
  language='js'
  dark
  format={false}
  code={dedent`
    const word = () => {
      renderDoc({
        list: [
          { img: '/demo.png', name: '图片1' },
          { img: '/demo.png', name: '图片2' }
        ],
        url: '/doc-template.docx'
      })
    }
  `}
/>